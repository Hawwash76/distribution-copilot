/**
 * @distribution-copilot/config
 *
 * Shared *runtime* configuration: app constants and the environment-variable
 * schema. (Build-time configs — ESLint, TypeScript, Prettier — live under
 * the top-level `tooling/` directory instead.)
 */
export * from "./constants";
export * from "./env";
