/**
 * @distribution-copilot/ui
 *
 * Shared, framework-agnostic React UI primitives. shadcn/ui is initialized
 * (see `components.json`) but no components are added yet — only the `cn`
 * helper ships today. Add generated components under `./components`.
 */
export * from "./lib/utils";
